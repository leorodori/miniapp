<div class="board-container">

  <!-- Word（タイトル） -->
  <div class="word-title">
    <%= @word.name %>
  </div>

  <!-- メモ一覧（カード表示） -->
  <div class="memo-list">
    <% @word.notes.each do |note| %>
      <div class="memo-card">
        <p class="memo-body"><%= note.body %></p>

        <div class="memo-actions flow">
          <%= link_to "編集", edit_word_note_path(@word, note), class: "btn" %>
          <%= link_to "削除", word_note_path(@word, note),
               data: { turbo_method: :delete, turbo_confirm: "本当に削除しますか？" },
               class: "btn-danger" %>
        </div>
      </div>
    <% end %>
  </div>

  <!-- メモ追加 -->
  <div class="memo-add">
    <%= link_to "新しいメモを追加する", new_word_note_path(@word), class: "btn" %>
  </div>

</div>